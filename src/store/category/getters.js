export const getters = {
  getMain: state => state.main_categories
  // getClinic: (state) => state.clinic,
  // getClinicTranslate: (state) => code => state.clinic.translate[code],
  // getFeaturedPhoto: (state) => state.clinic.FeaturedPhoto.url
};
